# Copy to .env and fill in values.
# EXPO_PUBLIC_ prefix makes these available in the app bundle.

# API base URL — use localhost in development, Railway URL in production
EXPO_PUBLIC_API_URL=http://localhost:3000
