# USER IDENTIFIED BUGS LIST

## CAPTION FUNCTION BROKEN

-CAPTION should be a block type that looks and behaves identical to character, but it doesn't
-Typing ca or cap in a CHARACTER block doesn't prompt the grey autocomplete text or the enter to complete funciton
-Selecting CAPTION as a type with tab doesn't trigger this behavior either, caption is basically a form of dialog. 
-Do we need a caption type that functions as a universal character. Do we also need a caption-dialog type that only exists beneath CAPTIONs, and isn't findable from tab?

## SUPPORT FOR NON-STANDARD IPAD KEYBOARD
-I am testing the app on an ipad mini, and the aftermarket keyboard i have has weird, non-standard key layout. I've attached a photo of they keys in the documents file: IMG1177.heic.
-Several of the shortcuts we mapped out seem broken, because the keys just don't exist. Is there a way to remap them so they would work on weird keyboards?
-Or maybe, would it be easier to just build a shortcut editor functionality in the app? This way any time there is a conflict, i can just remap the keys myself without having to rely on you to do it?

Keys that dont work:

whole app:
no escape button or back tic

Binder:
Up down for navigatin
shift up down for selection
enter for open entity
shift enter for split screen
left right for expand/collapse parent
type for filter
command shift a for new entity

timeline:
enter to advance and add an event

Script:
Any navigation with arrow keys broken.


Board:
-command arrow for navigation in board
